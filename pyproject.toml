[project]
name = "hexlet-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "Flask",
    "gunicorn",
    "python-dotenv",
    "psycopg2-binary==2.9.10",
    "validators",
    "requests",
    "beautifulsoup4",
]
